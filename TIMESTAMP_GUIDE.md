# 時間戳欄位詳細說明

## 📖 什麼是時間戳（Timestamp）？

時間戳是一個數字，用來表示從 **1970年1月1日 00:00:00 UTC**（也稱為 Unix 紀元）開始經過的毫秒數。

### 為什麼使用毫秒而不是秒？

- **秒級時間戳**：只能精確到秒（例如：`1737907200` = 2024-01-26 12:00:00）
- **毫秒級時間戳**：可以精確到毫秒（例如：`1737907200000` = 2024-01-26 12:00:00.000）

使用毫秒可以：
- ✅ 更精確地記錄時間
- ✅ 避免排序時的衝突（同一秒內多個產品）
- ✅ 符合 JavaScript/Flutter 的標準（`DateTime.now().millisecondsSinceEpoch`）

---

## 🎯 兩個時間戳欄位的用途

### 1. `releaseAtMs` - 發布時間戳

**用途**：
- 記錄產品**正式發布**的時間
- 用於判斷產品是否為「即將上架」（`releaseAt` 在未來）
- 用於「即將上架」區塊的排序（按發布時間升序）

**使用場景**：
```dart
// 在 home_sections_provider.dart 中
if (p.releaseAt != null) {
  if (p.releaseAt!.isAfter(now)) {
    isComing = true; // 標記為即將上架
  }
}

// 排序：有 releaseAt 的按 releaseAt，否則按 order
coming.sort((a, b) {
  if (a.releaseAt != null && b.releaseAt != null) {
    return a.releaseAt!.compareTo(b.releaseAt!); // 升序：越早發布越前面
  }
  // ...
});
```

**填寫建議**：
- 如果產品已經發布：填寫實際發布時間
- 如果產品尚未發布：填寫計劃發布時間（用於「即將上架」功能）
- 如果產品沒有特定發布時間：可以留空

### 2. `createdAtMs` - 建立時間戳

**用途**：
- 記錄產品**建立/創建**的時間
- 用於判斷產品是否為「本週新泡泡」（建立時間在過去7天內）
- 用於「本週新泡泡」區塊的排序（按建立時間降序，最新的在前）

**使用場景**：
```dart
// 在 v2_providers.dart 中
final from = now.subtract(const Duration(days: 7));
final list = map.values
    .where((p) => p.createdAt != null && p.createdAt!.isAfter(from))
    .toList(); // 篩選出過去7天內建立的產品

list.sort((a, b) => (b.createdAtMs ?? 0).compareTo(a.createdAtMs ?? 0));
// 降序：最新的在前
```

**填寫建議**：
- 填寫產品實際建立的時間
- 通常與資料上傳到系統的時間相同
- 用於「本週新泡泡」功能，建議填寫

---

## 📝 如何計算和填寫時間戳

### 方法 1：使用 Python（推薦）

```python
import time
from datetime import datetime

# 當前時間戳（毫秒）
current_ms = int(time.time() * 1000)
print(f"當前時間戳: {current_ms}")

# 指定日期時間戳（毫秒）
target_date = datetime(2024, 1, 26, 12, 0, 0)  # 2024-01-26 12:00:00
target_ms = int(target_date.timestamp() * 1000)
print(f"指定時間戳: {target_ms}")

# 從字串轉換
date_str = "2024-01-26 12:00:00"
date_obj = datetime.strptime(date_str, "%Y-%m-%d %H:%M:%S")
timestamp_ms = int(date_obj.timestamp() * 1000)
print(f"從字串轉換: {timestamp_ms}")
```

### 方法 2：使用 JavaScript/Node.js

```javascript
// 當前時間戳（毫秒）
const currentMs = Date.now();
console.log(`當前時間戳: ${currentMs}`);

// 指定日期時間戳（毫秒）
const targetDate = new Date('2024-01-26T12:00:00');
const targetMs = targetDate.getTime();
console.log(`指定時間戳: ${targetMs}`);

// 從字串轉換
const dateStr = "2024-01-26 12:00:00";
const dateObj = new Date(dateStr);
const timestampMs = dateObj.getTime();
console.log(`從字串轉換: ${timestampMs}`);
```

### 方法 3：使用線上工具

1. **Unix Timestamp Converter**
   - 網址：https://www.unixtimestamp.com/
   - 輸入日期時間 → 選擇「Milliseconds」→ 複製結果

2. **Epoch Converter**
   - 網址：https://www.epochconverter.com/
   - 輸入日期時間 → 複製「Milliseconds」欄位的值

### 方法 4：在 Excel 中計算

```excel
# 假設 A1 是日期時間（例如：2024-01-26 12:00:00）
# 在 B1 中輸入公式：
=(A1-DATE(1970,1,1))*86400000

# 或者使用 TEXT 函數格式化後再計算
=(DATEVALUE("2024-01-26")+TIMEVALUE("12:00:00")-DATE(1970,1,1))*86400000
```

**注意**：Excel 的日期計算可能因時區而異，建議使用 Python 或線上工具。

---

## 🔢 時間戳範例對照表

| 日期時間 | 時間戳（毫秒） | 說明 |
|---------|--------------|------|
| 2024-01-01 00:00:00 UTC | `1704067200000` | 2024年元旦 |
| 2024-01-26 12:00:00 UTC | `1737907200000` | 範例時間 |
| 2024-12-31 23:59:59 UTC | `1735689599000` | 2024年最後一秒 |
| 2025-01-01 00:00:00 UTC | `1735689600000` | 2025年元旦 |
| 現在（動態） | `Date.now()` | 當前時間 |

---

## ✅ 在 Excel 中填寫時間戳

### 步驟 1：計算時間戳

使用上述任一方法計算出時間戳數字。

### 步驟 2：在 Excel 中填入

1. 打開 `learning_bubble_template_new.xlsx`
2. 切換到 `PRODUCTS` 工作表
3. 找到 `releaseAtMs` 或 `createdAtMs` 欄位
4. **直接填入數字**（例如：`1737907200000`）
5. **不要填入日期格式**（例如：不要填 `2024-01-26`）

### 範例

```
productId    | releaseAtMs    | createdAtMs
-------------|----------------|----------------
product_001  | 1737907200000  | 1737907200000
product_002  | 1737993600000  | 1737907200000
product_003  |                | 1738080000000
```

**說明**：
- `product_001`：發布時間和建立時間相同（2024-01-26 12:00:00）
- `product_002`：發布時間較晚（2024-01-27 12:00:00），建立時間較早
- `product_003`：只有建立時間，沒有發布時間（留空）

---

## 🎨 實際應用場景

### 場景 1：新產品上架

```python
from datetime import datetime
import time

# 產品今天建立並立即發布
now_ms = int(time.time() * 1000)

# 在 Excel 中填入：
# releaseAtMs: {now_ms}
# createdAtMs: {now_ms}
```

### 場景 2：預告即將上架的產品

```python
from datetime import datetime, timedelta

# 產品今天建立，但計劃下週發布
created_ms = int(datetime.now().timestamp() * 1000)
release_ms = int((datetime.now() + timedelta(days=7)).timestamp() * 1000)

# 在 Excel 中填入：
# releaseAtMs: {release_ms}  # 未來時間
# createdAtMs: {created_ms}   # 現在時間
```

### 場景 3：歷史產品（沒有時間戳）

```python
# 如果產品是舊資料，沒有時間戳
# 可以留空，系統會使用 order 欄位排序
# releaseAtMs: (留空)
# createdAtMs: (留空)
```

---

## ⚠️ 常見錯誤和注意事項

### ❌ 錯誤 1：使用秒級時間戳

```python
# ❌ 錯誤：使用秒級時間戳
timestamp_seconds = int(time.time())  # 例如：1737907200

# ✅ 正確：使用毫秒級時間戳
timestamp_milliseconds = int(time.time() * 1000)  # 例如：1737907200000
```

### ❌ 錯誤 2：填入日期格式

```
# ❌ 錯誤：在 Excel 中填入日期格式
releaseAtMs: 2024-01-26

# ✅ 正確：填入數字
releaseAtMs: 1737907200000
```

### ❌ 錯誤 3：時區混淆

```python
# ⚠️ 注意：Python 的 datetime.now() 使用本地時區
# 如果需要 UTC 時間，使用：
from datetime import datetime, timezone

utc_now = datetime.now(timezone.utc)
utc_ms = int(utc_now.timestamp() * 1000)

# 或者明確指定時區
local_time = datetime(2024, 1, 26, 12, 0, 0)  # 本地時間
utc_time = local_time.replace(tzinfo=timezone.utc)  # 轉換為 UTC
utc_ms = int(utc_time.timestamp() * 1000)
```

### ❌ 錯誤 4：小數點

```python
# ❌ 錯誤：保留小數點
timestamp = time.time() * 1000  # 例如：1737907200.123

# ✅ 正確：轉換為整數
timestamp = int(time.time() * 1000)  # 例如：1737907200000
```

---

## 🔍 驗證時間戳是否正確

### 方法 1：使用 Python 驗證

```python
from datetime import datetime

# 假設時間戳是 1737907200000
timestamp_ms = 1737907200000

# 轉換回日期時間
dt = datetime.fromtimestamp(timestamp_ms / 1000)
print(f"日期時間: {dt}")  # 應該顯示：2024-01-26 12:00:00
```

### 方法 2：使用線上工具驗證

1. 訪問 https://www.unixtimestamp.com/
2. 在「Timestamp to Human date」欄位輸入時間戳
3. 選擇「Milliseconds」
4. 查看轉換後的日期時間是否正確

### 方法 3：在 Flutter/Dart 中驗證

```dart
// 假設時間戳是 1737907200000
final timestampMs = 1737907200000;
final dateTime = DateTime.fromMillisecondsSinceEpoch(timestampMs);
print('日期時間: $dateTime'); // 應該顯示：2024-01-26 12:00:00.000
```

---

## 📊 時間戳在系統中的使用流程

```
Excel 上傳
    ↓
upload_v3_excel.py 讀取 releaseAtMs / createdAtMs
    ↓
寫入 Firestore products/{productId}
    ↓
Flutter App 讀取 Product.fromDoc()
    ↓
轉換為 DateTime 物件：
  - releaseAt = DateTime.fromMillisecondsSinceEpoch(releaseAtMs)
  - createdAt = DateTime.fromMillisecondsSinceEpoch(createdAtMs)
    ↓
用於排序和篩選：
  - 本週新泡泡：createdAt 在過去7天內
  - 即將上架：releaseAt 在未來
```

---

## 💡 最佳實踐建議

1. **統一使用 UTC 時間**
   - 避免時區問題
   - 使用 `datetime.now(timezone.utc)` 或線上工具

2. **建立時間建議必填**
   - 用於「本週新泡泡」功能
   - 即使沒有發布時間，也建議填寫建立時間

3. **發布時間可選**
   - 只有需要「即將上架」功能時才填寫
   - 如果產品已發布且不需要預告功能，可以留空

4. **使用腳本自動化**
   - 建立 Python 腳本自動計算時間戳
   - 避免手動計算錯誤

5. **驗證後再上傳**
   - 上傳前使用驗證方法檢查時間戳
   - 確保日期時間正確

---

## 🛠️ 實用工具腳本

### Python 腳本：批量計算時間戳

```python
#!/usr/bin/env python3
"""批量計算時間戳工具"""
from datetime import datetime

def calculate_timestamps(date_str, time_str="00:00:00"):
    """計算指定日期時間的時間戳（毫秒）"""
    dt_str = f"{date_str} {time_str}"
    dt = datetime.strptime(dt_str, "%Y-%m-%d %H:%M:%S")
    timestamp_ms = int(dt.timestamp() * 1000)
    return timestamp_ms

# 使用範例
if __name__ == "__main__":
    # 計算 2024-01-26 12:00:00 的時間戳
    ts = calculate_timestamps("2024-01-26", "12:00:00")
    print(f"時間戳: {ts}")  # 輸出：1737907200000
    
    # 驗證
    dt = datetime.fromtimestamp(ts / 1000)
    print(f"驗證: {dt}")  # 輸出：2024-01-26 12:00:00:000000
```

---

## 📞 問題排查

### Q1: 時間戳填寫後，產品沒有出現在「本週新泡泡」？

**檢查**：
1. 確認 `createdAtMs` 是否填寫
2. 確認時間戳是否為毫秒（13位數字）
3. 確認時間戳是否在過去7天內
4. 檢查 Firestore 中資料是否正確寫入

### Q2: 時間戳填寫後，產品沒有出現在「即將上架」？

**檢查**：
1. 確認 `releaseAtMs` 是否填寫
2. 確認時間戳是否為未來時間
3. 確認 `published` 是否為 `false`（未發布）

### Q3: 時間戳轉換後的日期時間不對？

**檢查**：
1. 確認是否使用毫秒（不是秒）
2. 確認時區設定（建議使用 UTC）
3. 使用驗證工具檢查時間戳是否正確

---

## 📚 參考資源

- [Unix 時間戳維基百科](https://zh.wikipedia.org/wiki/Unix時間)
- [Python datetime 文檔](https://docs.python.org/3/library/datetime.html)
- [JavaScript Date 文檔](https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/Date)
- [線上時間戳轉換工具](https://www.unixtimestamp.com/)

---

**最後更新**：2024-01-26
